# Runtime dependencies, PINNED so local == CI == container (the project's
# version-parity rule). Bump deliberately, not by floating to latest.
fastapi==0.139.2
uvicorn[standard]==0.51.0
boto3==1.43.53
pydantic==2.13.4
python-dotenv==1.2.2
